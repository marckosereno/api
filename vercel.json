{
  "installCommand": "npm install",
  "buildCommand": "echo 'No build step required'",
  "functions": {
    "api/*.js": {
      "runtime": "nodejs20.x" 
      // Usamos el formato simple "nodejs20.x" para evitar el error de "valid version"
    }
  }
}
